import { createSlice } from "@reduxjs/toolkit";


export const studentSlice = createSlice({
    name: "studdents",
    initialState: {value: [] },
    reducers: {
        getUser: (state, action) => {
            
        }
    }
})